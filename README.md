# Internship-Day-3-HTML-
This webpage is designed using a Flexbox parent-child structure where a flex container controls its flex items. It includes a top navigation bar, a flexible middle section for sidebar and main content, and a footer at the bottom. The layout adapts smoothly to different content sizes without breaking.
